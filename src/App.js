import React, { Component } from "react";
import { Button } from "./build/lib";

class App extends Component {
  render() {
    return (
      <div>
        <Button>Button</Button>
      </div>
    );
  }
}

export default App;
